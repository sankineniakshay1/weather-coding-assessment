# weather-coding-assessment
Coding assessment: .NET 8 web app that reads dates from dates.txt, fetches Dallas historical weather from Open-Meteo (min/max temperature and precipitation), caches raw JSON under weather-data/, exposes GET /api/weather, and provides a simple UI with sorting/filtering and error handling.
